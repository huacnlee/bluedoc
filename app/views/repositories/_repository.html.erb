<li class="list-item repository-item">
  <div class="title">
    <% if defined?(current_group) && current_group.id != repository.user_id %>
    <%= user_name_tag(repository.user) %> <span class="divider">/</span>
    <% end %>
    <%= repository_name_tag(repository) %>
    <% if repository.private? %>
      <span class="label label-private ml-2"><%= t(".Private") %></span>
    <% end %>
  </div>
  <div class="description">
    <%= truncate repository.description, length: 150 %>
  </div>
  <div class="info">
    <span class="stars">
      <%= icon_tag("star") %> <%= repository.stars_count %>
    </span>
  </div>
  <div class="time-box">
    <%= timeago repository.updated_at %>
  </div>
</li>
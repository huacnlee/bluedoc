<% title_tag t(".Docs"), @repository.name %>

<%= render "menu" %>


<%= react_component "repositories/DocList", {
  repositoryId: @repository.id,
  userId: @user.id,
  sort: params[:sort],
  abilities: {
    update: can?(:update, @repository),
    destroy: can?(:destroy, Doc.new(repository_id: @repository.id)),
  },
} %>

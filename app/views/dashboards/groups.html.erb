<% title_tag "Groups", "Dashboard" %>

<div class="col-12 d-table">
  <%= render "menu" %>

  <div class="col-10 d-table-cell p-5">
    <div class="sub-title">Recent groups</div>
    <%= render partial: "groups", locals: { items: @user_actives } %>
    <%= paginate @user_actives %>
  </div>
</div>

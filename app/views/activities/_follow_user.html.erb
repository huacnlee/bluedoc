<div class="info">
  <%= user_name_tag(activity.actor) %>
  <%= t("activities.has followed") %>
  <%= user_name_tag(activity.target) %>
</div>
<span class="time"><%= timeago activity.created_at %></span>
<div class="detail">
  <%= render "/users/card", user: activity.target %>
</div>

<div class="user-following">
  <% if @users.blank? %>
    <div class="blankslate">
      <p><%= @user.name %> not has following any one</p>
    </div>
  </div>
  <% else %>
    <%= render @users %>
    <%= paginate @users %>
  <% end %>
</div>

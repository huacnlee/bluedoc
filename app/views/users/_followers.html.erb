<div class="user-followers">
  <% if @users.blank? %>
    <div class="blankslate">
      <p><%= @user.name %> not has followers</p>
    </div>
  <% else %>
    <ul class="list-container">
      <%= render @users %>
    </ul>
    <%= paginate @users %>
  <% end %>
</div>
